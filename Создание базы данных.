Создание базы данных.

CREATE DATABASE IF NOT EXISTS test;

Создание таблицы

CREATE TABLE users (
    id INT(6) AUTO_INCREMENT PRIMARY KEY,
    firstname VARCHAR(30) NOT NULL,
    lastname VARCHAR(30) NOT NULL
) ENGINE = InnoDB;

CREATE TABLE comments (
    id INT(6) AUTO_INCREMENT PRIMARY KEY,
    text VARCHAR(150) NOT NULL,
    user_id INT NOT NULL
) ENGINE = InnoDB;

Добавление строки в таблицу

INSERT INTO users (firstname, lastname) VALUES ('John', 'Smith');
INSERT INTO users (firstname, lastname) VALUES ('Jack', 'Dawson');
INSERT INTO users (firstname, lastname) VALUES ('Liza', 'Simpson');
INSERT INTO users (firstname, lastname) VALUES ('Homer', 'Simpson');
INSERT INTO users (id, firstname, lastname) VALUES (10, 'Harry', 'Potter');

INSERT INTO comments (text, user_id) VALUES ('Bla', 1);
INSERT INTO comments (text, user_id) VALUES ('Bla1', 2);
INSERT INTO comments (text, user_id) VALUES ('Bla2', 1);
INSERT INTO comments (text, user_id) VALUES ('Bla3', 2);
INSERT INTO comments (text, user_id) VALUES ('Bla4', 3);
INSERT INTO comments (text, user_id) VALUES ('Bla5', 4);

Получить все строки из таблицы
SELECT * FROM users;

Получить только одно поле
SELECT firstname FROM users;

Получить все данные о юзере с id = 4
SELECT * FROM users WHERE id=4;

SELECT users.id as crocodile, users.firstname, users.lastname FROM users JOIN comments on comments.user_id=users.id WHERE comments.id=4;

SELECT * FROM comments JOIN users on comments.user_id=users.id;